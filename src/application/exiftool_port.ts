/**
 * @deprecated Test-only compatibility marker for legacy fakes. Production code must
 * depend on MetadataEnginePort; this alias deliberately exposes no CLI arguments.
 */
export interface ExifToolPort {}
